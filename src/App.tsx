import { BrowserRouter } from 'react-router-dom';
import Router from './components/Router';

const App = ()  => {
	return (
		<BrowserRouter>
			<Router></Router>
		</BrowserRouter>
	);
}

export default App;
