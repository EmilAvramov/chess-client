import styles from '../../styles/components/FAQ.module.scss';

const FAQ = () => {
	return (
		<main className={styles['faq__wrapper']}>
			<div>About works!</div>
		</main>
	);
};

export default FAQ;
