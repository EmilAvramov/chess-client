import styles from '../../styles/components/Home.module.scss';

const Home = () => {
	return (
		<main className={styles['home__wrapper']}>
			<div>Home works!</div>
		</main>
	);
};

export default Home;
